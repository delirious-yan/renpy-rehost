# Auto-detect text files and normalize line endings to LF in the repo.
* text=auto eol=lf

# Windows batch files need CRLF to run reliably.
*.cmd text eol=crlf
*.bat text eol=crlf

# Never diff/merge these as text.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.exe binary
*.dll binary
